name: Download Kaggle Dataset

on:
  workflow_dispatch:
    inputs:
      dataset:
        description: 'Kaggle dataset slug (e.g. marquis03/bdd100k)'
        required: true
        default: 'marquis03/bdd100k'

jobs:
  download-and-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Kaggle CLI
        run: pip install kaggle

      - name: Configure Kaggle credentials
        env:
          KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
          KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
        run: |
          mkdir -p ~/.kaggle
          echo "{\"username\":\"$KAGGLE_USERNAME\",\"key\":\"$KAGGLE_KEY\"}" > ~/.kaggle/kaggle.json
          chmod 600 ~/.kaggle/kaggle.json

      - name: Download dataset
        run: |
          mkdir -p data
          kaggle datasets download \
            -d "${{ github.event.inputs.dataset }}" \
            -p data \
            --unzip \
            --force

      - name: Commit and Push in batches
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config --global http.postBuffer 524288000
          
          BATCH_LIMIT=$((100 * 1024 * 1024))
          batch_size=0
          batch_num=1
          files_in_batch=()

          while IFS= read -r -d '' file; do
            file_size=$(stat -c%s "$file")
            
            if [ "$file_size" -gt "$BATCH_LIMIT" ]; then
              if [ ${#files_in_batch[@]} -gt 0 ]; then
                git add "${files_in_batch[@]}"
                git commit -m "Batch $batch_num (${{ github.event.inputs.dataset }})"
                git push
                batch_num=$((batch_num + 1))
                files_in_batch=()
                batch_size=0
              fi
              git add "$file"
              git commit -m "Large file $(basename "$file") (${{ github.event.inputs.dataset }})"
              git push
              continue
            fi

            files_in_batch+=("$file")
            batch_size=$((batch_size + file_size))

            if [ "$batch_size" -ge "$BATCH_LIMIT" ]; then
              git add "${files_in_batch[@]}"
              git commit -m "Batch $batch_num (${{ github.event.inputs.dataset }})"
              git push
              batch_num=$((batch_num + 1))
              files_in_batch=()
              batch_size=0
            fi
          done < <(git -C . status --porcelain -z -- data | grep -z -E '^(\?\?|.M| M)' | cut -z -c4- | tr '\0' '\0')

          if [ ${#files_in_batch[@]} -gt 0 ]; then
            git add "${files_in_batch[@]}"
            git commit -m "Final batch $batch_num (${{ github.event.inputs.dataset }})"
            git push
          fi
